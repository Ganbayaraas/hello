angular.module("car_collateral.Ctrl", []).controller("car_collateralCtrl", function ($ionicSlideBoxDelegate, $scope, $filter) {
  // $scope.toAmount = function () {
  // console.log("work - 2", $scope.amount.toString().replace(/\B(?=(\d{3})+(?!\d))/g, ","));
  // $scope.amount = $filter("currency")($scope.amount, "");
  // $scope.amount1 = $scope.amount.toString().replace(/\B(?=(\d{3})+(?!\d))/g, ",");
  // };
});
